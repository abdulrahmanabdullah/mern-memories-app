call api session in app component.
get user if exist and keep it login by session id 
if not return null and wait until user login or register.

in route
app.get("/user/sessions", getUserSessions)
in control 
getUserSessions looking for user in browser sessions and return user payload.
if sessions is empty return null 

localhost:client -ask user profile from ->
localhost:server -talk to  middleware to -> save session id in locals server
extract session id from locals server then pass it to database 
Database find user by session id 
return user with response to localhost:client  
